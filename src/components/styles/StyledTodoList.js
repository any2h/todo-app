import styled from "styled-components";

export const StyledTodoList = styled.section`
    position: relative;

    > div {
        display: flex;
        position: absolute;
        top: -68px;
        left: 24px;
    }
`
